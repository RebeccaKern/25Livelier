<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location_address %>
    <%= form.text_field :location_address, id: :room_location_address %>
  </div>

  <div class="field">
    <%= form.label :max_capacity %>
    <%= form.number_field :max_capacity, id: :room_max_capacity %>
  </div>

  <div class="field">
    <%= form.label :type %>
    <%= form.text_field :type, id: :room_type %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :room_description %>
  </div>

  <div class="field">
    <%= form.label :building_id %>
    <%= form.text_field :building_id, id: :room_building_id %>
  </div>

  <div class="actions btn waves-effect waves-light">
    <%= form.submit %>
  </div>
<% end %>
